export function setButtonText(
  btn,
  isLoading,
  defaultText = "Save",
  loadingText = "Saving...",
) {
  if (isLoading) {
    // Set the loading text
  } else {
    // Set not loading text
  }
}
